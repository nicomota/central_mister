import { environment } from '../../../environments/environment';

export const VERSION = environment.VERSION;
export const DEBUG_INFO_ENABLED = environment.DEBUG_INFO_ENABLED;
export const BUILD_TIMESTAMP = environment.BUILD_TIMESTAMP;
export const AMBIENTE = environment.AMBIENTE;
export const AUTH_URL = environment.AUTH_URL;
